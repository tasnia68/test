version: '3.8'

services:
  db:
    image: postgis/postgis:15-3.3
    container_name: ainbondhu_db
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: password
      POSTGRES_DB: ainbondhu
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

  redis:
    image: redis:alpine
    container_name: ainbondhu_redis
    ports:
      - "6379:6379"

volumes:
  postgres_data:
